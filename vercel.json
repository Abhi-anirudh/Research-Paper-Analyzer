{
  "version": 2,
  "framework": "angular",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist/research-paper-analyzer/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
